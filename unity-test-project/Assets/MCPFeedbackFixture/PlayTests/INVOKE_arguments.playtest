# @needs editmode
INCLUDE common.defs
INVOKE /Fixture/State FixtureState ResetState
INVOKE /Fixture/State FixtureState Record "hello world","second arg",42
ASSERT $last_first == hello world
ASSERT $last_second == second arg
ASSERT $last_int == 42
