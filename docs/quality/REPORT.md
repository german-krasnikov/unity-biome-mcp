# Quality Report

> Auto-generated on **2026-08-06** from commit `4c0b183` (v1.20.0)

## Project Overview

| Metric | Value |
|--------|-------|
| Version | v1.20.0 |
| Commit | `4c0b183` |
| Date | 2026-08-06 |
| MCP Tools | 148 |

## Test Results

| Suite | Passed | Failed | Skipped | Total | Status |
|-------|--------|--------|---------|-------|--------|
| Python Server (3.12) | 4605 | 0 | 3 | 4608 | ✅ |
| Python Install | 76 | 0 | 0 | 76 | ✅ |
| Python Scripts | 347 | 0 | 0 | 347 | ✅ |
| C# EditMode (Linux) | 7087 | 0 | 160 | 7247 | ✅ |
| C# EditMode (Windows) | 6974 | 0 | 273 | 7247 | ✅ |
| C# EditMode (macOS) | 7087 | 0 | 160 | 7247 | ✅ |

## Tool Quality

| Linter | Errors | Warnings | Score |
|--------|--------|----------|-------|
| mcp-tool-card-linter | 19 | 597 | 72.8/100 |
| mcp-lint | 699 | 1162 | — |

### Per-Tool Scores

<details>
<summary>148 tools scored (click to expand)</summary>

| Tool | Score | Errors | Warnings | Risk |
|------|-------|--------|----------|------|
| `?` | 18 | 0 | 0 | — |
| `?` | 20 | 0 | 0 | — |
| `?` | 37 | 0 | 0 | — |
| `?` | 38 | 0 | 0 | — |
| `?` | 40 | 0 | 0 | — |
| `?` | 44 | 0 | 0 | — |
| `?` | 45 | 0 | 0 | — |
| `?` | 46 | 0 | 0 | — |
| `?` | 48 | 0 | 0 | — |
| `?` | 51 | 0 | 0 | — |
| `?` | 52 | 0 | 0 | — |
| `?` | 52 | 0 | 0 | — |
| `?` | 53 | 0 | 0 | — |
| `?` | 53 | 0 | 0 | — |
| `?` | 53 | 0 | 0 | — |
| `?` | 55 | 0 | 0 | — |
| `?` | 56 | 0 | 0 | — |
| `?` | 57 | 0 | 0 | — |
| `?` | 57 | 0 | 0 | — |
| `?` | 58 | 0 | 0 | — |
| `?` | 58 | 0 | 0 | — |
| `?` | 58 | 0 | 0 | — |
| `?` | 61 | 0 | 0 | — |
| `?` | 61 | 0 | 0 | — |
| `?` | 61 | 0 | 0 | — |
| `?` | 62 | 0 | 0 | — |
| `?` | 62 | 0 | 0 | — |
| `?` | 64 | 0 | 0 | — |
| `?` | 64 | 0 | 0 | — |
| `?` | 64 | 0 | 0 | — |
| `?` | 64 | 0 | 0 | — |
| `?` | 64 | 0 | 0 | — |
| `?` | 64 | 0 | 0 | — |
| `?` | 65 | 0 | 0 | — |
| `?` | 66 | 0 | 0 | — |
| `?` | 66 | 0 | 0 | — |
| `?` | 67 | 0 | 0 | — |
| `?` | 67 | 0 | 0 | — |
| `?` | 67 | 0 | 0 | — |
| `?` | 67 | 0 | 0 | — |
| `?` | 68 | 0 | 0 | — |
| `?` | 68 | 0 | 0 | — |
| `?` | 68 | 0 | 0 | — |
| `?` | 68 | 0 | 0 | — |
| `?` | 69 | 0 | 0 | — |
| `?` | 70 | 0 | 0 | — |
| `?` | 70 | 0 | 0 | — |
| `?` | 70 | 0 | 0 | — |
| `?` | 71 | 0 | 0 | — |
| `?` | 71 | 0 | 0 | — |
| `?` | 71 | 0 | 0 | — |
| `?` | 72 | 0 | 0 | — |
| `?` | 72 | 0 | 0 | — |
| `?` | 72 | 0 | 0 | — |
| `?` | 72 | 0 | 0 | — |
| `?` | 73 | 0 | 0 | — |
| `?` | 73 | 0 | 0 | — |
| `?` | 73 | 0 | 0 | — |
| `?` | 73 | 0 | 0 | — |
| `?` | 73 | 0 | 0 | — |
| `?` | 74 | 0 | 0 | — |
| `?` | 74 | 0 | 0 | — |
| `?` | 74 | 0 | 0 | — |
| `?` | 74 | 0 | 0 | — |
| `?` | 74 | 0 | 0 | — |
| `?` | 74 | 0 | 0 | — |
| `?` | 74 | 0 | 0 | — |
| `?` | 75 | 0 | 0 | — |
| `?` | 75 | 0 | 0 | — |
| `?` | 75 | 0 | 0 | — |
| `?` | 75 | 0 | 0 | — |
| `?` | 76 | 0 | 0 | — |
| `?` | 76 | 0 | 0 | — |
| `?` | 76 | 0 | 0 | — |
| `?` | 76 | 0 | 0 | — |
| `?` | 76 | 0 | 0 | — |
| `?` | 76 | 0 | 0 | — |
| `?` | 76 | 0 | 0 | — |
| `?` | 77 | 0 | 0 | — |
| `?` | 77 | 0 | 0 | — |
| `?` | 77 | 0 | 0 | — |
| `?` | 77 | 0 | 0 | — |
| `?` | 78 | 0 | 0 | — |
| `?` | 78 | 0 | 0 | — |
| `?` | 78 | 0 | 0 | — |
| `?` | 78 | 0 | 0 | — |
| `?` | 78 | 0 | 0 | — |
| `?` | 78 | 0 | 0 | — |
| `?` | 78 | 0 | 0 | — |
| `?` | 78 | 0 | 0 | — |
| `?` | 78 | 0 | 0 | — |
| `?` | 78 | 0 | 0 | — |
| `?` | 78 | 0 | 0 | — |
| `?` | 78 | 0 | 0 | — |
| `?` | 78 | 0 | 0 | — |
| `?` | 78 | 0 | 0 | — |
| `?` | 79 | 0 | 0 | — |
| `?` | 79 | 0 | 0 | — |
| `?` | 80 | 0 | 0 | — |
| `?` | 80 | 0 | 0 | — |
| `?` | 80 | 0 | 0 | — |
| `?` | 80 | 0 | 0 | — |
| `?` | 80 | 0 | 0 | — |
| `?` | 80 | 0 | 0 | — |
| `?` | 80 | 0 | 0 | — |
| `?` | 80 | 0 | 0 | — |
| `?` | 80 | 0 | 0 | — |
| `?` | 81 | 0 | 0 | — |
| `?` | 81 | 0 | 0 | — |
| `?` | 81 | 0 | 0 | — |
| `?` | 82 | 0 | 0 | — |
| `?` | 82 | 0 | 0 | — |
| `?` | 82 | 0 | 0 | — |
| `?` | 82 | 0 | 0 | — |
| `?` | 82 | 0 | 0 | — |
| `?` | 82 | 0 | 0 | — |
| `?` | 82 | 0 | 0 | — |
| `?` | 82 | 0 | 0 | — |
| `?` | 83 | 0 | 0 | — |
| `?` | 83 | 0 | 0 | — |
| `?` | 83 | 0 | 0 | — |
| `?` | 83 | 0 | 0 | — |
| `?` | 86 | 0 | 0 | — |
| `?` | 86 | 0 | 0 | — |
| `?` | 87 | 0 | 0 | — |
| `?` | 87 | 0 | 0 | — |
| `?` | 87 | 0 | 0 | — |
| `?` | 87 | 0 | 0 | — |
| `?` | 87 | 0 | 0 | — |
| `?` | 87 | 0 | 0 | — |
| `?` | 87 | 0 | 0 | — |
| `?` | 89 | 0 | 0 | — |
| `?` | 89 | 0 | 0 | — |
| `?` | 89 | 0 | 0 | — |
| `?` | 89 | 0 | 0 | — |
| `?` | 89 | 0 | 0 | — |
| `?` | 89 | 0 | 0 | — |
| `?` | 89 | 0 | 0 | — |
| `?` | 89 | 0 | 0 | — |
| `?` | 89 | 0 | 0 | — |
| `?` | 89 | 0 | 0 | — |
| `?` | 89 | 0 | 0 | — |
| `?` | 89 | 0 | 0 | — |
| `?` | 89 | 0 | 0 | — |
| `?` | 89 | 0 | 0 | — |
| `?` | 89 | 0 | 0 | — |
| `?` | 89 | 0 | 0 | — |
| `?` | 89 | 0 | 0 | — |

</details>

### Cross-Client Issues (mcp-lint)

<details>
<summary>Issues by tool (click to expand)</summary>

#### `alias_status`

- ⚠ Tool "alias_status" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "alias_status" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `analyze_lod_culling`

- ⚠ Parameter "focus" in tool "analyze_lod_culling" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.focus" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "focus" in tool "analyze_lod_culling" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.focus" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "analyze_lod_culling" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "analyze_lod_culling" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `animation`

- ⚠ Parameter "action" in tool "animation" is missing a description.
- ⚠ Parameter "path" in tool "animation" is missing a description.
- ⚠ Parameter "clip" in tool "animation" is missing a description.
- ⚠ Parameter "clip_name" in tool "animation" is missing a description.
- ⚠ Parameter "property" in tool "animation" is missing a description.
- ⚠ Parameter "keys" in tool "animation" is missing a description.
- ⚠ Parameter "time" in tool "animation" is missing a description.
- ⚠ Parameter "component_type" in tool "animation" is missing a description.
- ⚠ Parameter "binding_path" in tool "animation" is missing a description.
- ⚠ Parameter "tangent" in tool "animation" is missing a description.
- ⚠ Parameter "function_name" in tool "animation" is missing a description.
- ⚠ Parameter "int_param" in tool "animation" is missing a description.
- ⚠ Parameter "float_param" in tool "animation" is missing a description.
- ⚠ Parameter "string_param" in tool "animation" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.clip" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.clip_name" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.property" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.keys" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.time" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.component_type" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.binding_path" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.tangent" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.function_name" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.int_param" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.float_param" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.string_param" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "clip" in tool "animation" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "clip_name" in tool "animation" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "property" in tool "animation" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "keys" in tool "animation" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "time" in tool "animation" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "component_type" in tool "animation" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "binding_path" in tool "animation" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "tangent" in tool "animation" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "function_name" in tool "animation" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "int_param" in tool "animation" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "float_param" in tool "animation" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "string_param" in tool "animation" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.clip" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.clip_name" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.property" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.keys" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.time" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.component_type" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.binding_path" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.tangent" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.function_name" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.int_param" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.float_param" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.string_param" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "animation" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "animation" description is 954 characters. Cline truncates at 200.
- ✖ Tool "animation" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.
- ✖ Schema at "inputSchema.properties.int_param.anyOf[0]" uses type "integer" which is not allowed by OpenAI Agents SDK. Use one of: string, number, boolean, object, array, null.

#### `animator`

- ⚠ Parameter "action" in tool "animator" is missing a description.
- ⚠ Parameter "path" in tool "animator" is missing a description.
- ⚠ Parameter "state" in tool "animator" is missing a description.
- ⚠ Parameter "states" in tool "animator" is missing a description.
- ⚠ Parameter "params" in tool "animator" is missing a description.
- ⚠ Parameter "source" in tool "animator" is missing a description.
- ⚠ Parameter "target" in tool "animator" is missing a description.
- ⚠ Parameter "conditions" in tool "animator" is missing a description.
- ⚠ Parameter "duration" in tool "animator" is missing a description.
- ⚠ Parameter "exit_time" in tool "animator" is missing a description.
- ⚠ Parameter "has_exit_time" in tool "animator" is missing a description.
- ⚠ Parameter "type" in tool "animator" is missing a description.
- ⚠ Parameter "name" in tool "animator" is missing a description.
- ⚠ Parameter "blend_type" in tool "animator" is missing a description.
- ⚠ Parameter "param" in tool "animator" is missing a description.
- ⚠ Parameter "param_y" in tool "animator" is missing a description.
- ⚠ Parameter "children" in tool "animator" is missing a description.
- ⚠ Parameter "edit_action" in tool "animator" is missing a description.
- ⚠ Parameter "layer" in tool "animator" is missing a description.
- ⚠ Parameter "weight" in tool "animator" is missing a description.
- ⚠ Parameter "blending" in tool "animator" is missing a description.
- ⚠ Parameter "value" in tool "animator" is missing a description.
- ⚠ Parameter "avatar_path" in tool "animator" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.state" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.states" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.params" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.source" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.target" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.conditions" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.duration" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.exit_time" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.has_exit_time" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.type" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.name" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.blend_type" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.param" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.param_y" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.children" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.edit_action" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.layer" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.weight" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.blending" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.value" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.avatar_path" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "state" in tool "animator" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "states" in tool "animator" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "params" in tool "animator" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "source" in tool "animator" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "target" in tool "animator" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "conditions" in tool "animator" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "duration" in tool "animator" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "exit_time" in tool "animator" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "has_exit_time" in tool "animator" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "type" in tool "animator" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "name" in tool "animator" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "blend_type" in tool "animator" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "param" in tool "animator" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "param_y" in tool "animator" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "children" in tool "animator" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "edit_action" in tool "animator" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "layer" in tool "animator" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "weight" in tool "animator" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "blending" in tool "animator" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "value" in tool "animator" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "avatar_path" in tool "animator" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.state" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.states" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.params" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.source" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.target" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.conditions" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.duration" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.exit_time" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.has_exit_time" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.type" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.name" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.blend_type" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.param" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.param_y" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.children" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.edit_action" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.layer" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.weight" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.blending" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.value" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.avatar_path" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "animator" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "animator" has 23 parameters. VS Code Copilot performance degrades with more than 15. Consider splitting into multiple tools.
- ⚠ Schema at "inputSchema.properties.layer" uses `anyOf` with 3 variants. Windsurf supports at most 2.
- ⚠ Tool "animator" description is 1122 characters. Cline truncates at 200.
- ✖ Tool "animator" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.
- ✖ Schema at "inputSchema.properties.layer.anyOf[0]" uses type "integer" which is not allowed by OpenAI Agents SDK. Use one of: string, number, boolean, object, array, null.

#### `animator_intent`

- ⚠ Parameter "target" in tool "animator_intent" is missing a description.
- ⚠ Parameter "intent" in tool "animator_intent" is missing a description.
- ⚠ Parameter "dry_run" in tool "animator_intent" is missing a description.
- ⚠ Parameter "dry_run" in tool "animator_intent" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Tool "animator_intent" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "animator_intent" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `apply_scene_change`

- ⚠ Parameter "plan_id" in tool "apply_scene_change" is missing a description.
- ⚠ Parameter "commands" in tool "apply_scene_change" is missing a description.
- ⚠ Parameter "verify" in tool "apply_scene_change" is missing a description.
- ⚠ Parameter "save" in tool "apply_scene_change" is missing a description.
- ⚠ Parameter "verify" in tool "apply_scene_change" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "save" in tool "apply_scene_change" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Tool "apply_scene_change" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "apply_scene_change" description is 271 characters. Cline truncates at 200.
- ✖ Tool "apply_scene_change" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `apply_template`

- ⚠ Parameter "name" in tool "apply_template" is missing a description.
- ⚠ Parameter "params" in tool "apply_template" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.params" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "params" in tool "apply_template" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.params" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "apply_template" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "apply_template" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `ask`

- ⚠ Parameter "question" in tool "ask" is missing a description.
- ⚠ Tool "ask" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "ask" description is 265 characters. Cline truncates at 200.
- ✖ Tool "ask" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `ask_user`

- ⚠ Parameter "questions" in tool "ask_user" is missing a description.
- ⚠ Tool "ask_user" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "ask_user" description is 432 characters. Cline truncates at 200.
- ✖ Tool "ask_user" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `asset`

- ⚠ Parameter "action" in tool "asset" is missing a description.
- ⚠ Parameter "path" in tool "asset" is missing a description.
- ⚠ Parameter "type" in tool "asset" is missing a description.
- ⚠ Parameter "name" in tool "asset" is missing a description.
- ⚠ Parameter "folder" in tool "asset" is missing a description.
- ⚠ Parameter "source" in tool "asset" is missing a description.
- ⚠ Parameter "dest" in tool "asset" is missing a description.
- ⚠ Parameter "prop" in tool "asset" is missing a description.
- ⚠ Parameter "value" in tool "asset" is missing a description.
- ⚠ Parameter "recursive" in tool "asset" is missing a description.
- ⚠ Parameter "labels" in tool "asset" is missing a description.
- ⚠ Parameter "output" in tool "asset" is missing a description.
- ⚠ Parameter "include_deps" in tool "asset" is missing a description.
- ⚠ Parameter "content" in tool "asset" is missing a description.
- ⚠ Parameter "class_name" in tool "asset" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.path" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.type" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.name" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.folder" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.source" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.dest" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.prop" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.value" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.labels" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.output" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.content" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.class_name" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "path" in tool "asset" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "type" in tool "asset" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "name" in tool "asset" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "folder" in tool "asset" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "source" in tool "asset" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "dest" in tool "asset" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "prop" in tool "asset" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "value" in tool "asset" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "recursive" in tool "asset" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "labels" in tool "asset" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "output" in tool "asset" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "include_deps" in tool "asset" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "content" in tool "asset" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "class_name" in tool "asset" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.path" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.type" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.name" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.folder" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.source" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.dest" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.prop" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.value" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.labels" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.output" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.content" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.class_name" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "asset" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "asset" description is 647 characters. Cline truncates at 200.
- ✖ Tool "asset" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `auto_fix`

- ⚠ Tool "auto_fix" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "auto_fix" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `auto_wire`

- ⚠ Parameter "path" in tool "auto_wire" is missing a description.
- ⚠ Parameter "dry_run" in tool "auto_wire" is missing a description.
- ⚠ Parameter "dry_run" in tool "auto_wire" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Tool "auto_wire" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "auto_wire" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `autofit_collider`

- ⚠ Parameter "path" in tool "autofit_collider" is missing a description.
- ⚠ Parameter "type" in tool "autofit_collider" is missing a description.
- ⚠ Parameter "type" in tool "autofit_collider" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Tool "autofit_collider" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "autofit_collider" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `await_compile`

- ⚠ Parameter "timeout" in tool "await_compile" is missing a description.
- ⚠ Parameter "timeout" in tool "await_compile" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Tool "await_compile" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "await_compile" description is 355 characters. Cline truncates at 200.
- ✖ Tool "await_compile" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `bake`

- ⚠ Parameter "target" in tool "bake" is missing a description.
- ⚠ Parameter "action" in tool "bake" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.action" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "action" in tool "bake" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.action" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "bake" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "bake" description is 223 characters. Cline truncates at 200.
- ✖ Tool "bake" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `batch`

- ⚠ Parameter "commands" in tool "batch" is missing a description.
- ⚠ Parameter "on_error" in tool "batch" is missing a description.
- ⚠ Parameter "timeout" in tool "batch" is missing a description.
- ⚠ Parameter "atomic" in tool "batch" is missing a description.
- ⚠ Parameter "validate_aliases" in tool "batch" is missing a description.
- ⚠ Parameter "on_error" in tool "batch" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "timeout" in tool "batch" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "atomic" in tool "batch" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "validate_aliases" in tool "batch" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Tool "batch" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "batch" description is 332 characters. Cline truncates at 200.
- ✖ Tool "batch" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `budget_status`

- ⚠ Tool "budget_status" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "budget_status" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `build`

- ⚠ Parameter "action" in tool "build" is missing a description.
- ⚠ Parameter "target" in tool "build" is missing a description.
- ⚠ Parameter "scenes" in tool "build" is missing a description.
- ⚠ Parameter "path" in tool "build" is missing a description.
- ⚠ Parameter "dev" in tool "build" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.target" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.scenes" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.path" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "target" in tool "build" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "scenes" in tool "build" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "path" in tool "build" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "dev" in tool "build" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.target" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.scenes" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.path" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "build" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "build" description is 260 characters. Cline truncates at 200.
- ✖ Tool "build" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `cancel_test_run`

- ⚠ Parameter "run_id" in tool "cancel_test_run" is missing a description.
- ⚠ Tool "cancel_test_run" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "cancel_test_run" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `check_colliders`

- ⚠ Parameter "path" in tool "check_colliders" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.path" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "path" in tool "check_colliders" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.path" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "check_colliders" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "check_colliders" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `checkpoint`

- ⚠ Parameter "label" in tool "checkpoint" is missing a description.
- ⚠ Parameter "label" in tool "checkpoint" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Tool "checkpoint" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "checkpoint" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `compile_preflight`

- ⚠ Parameter "file_path" in tool "compile_preflight" is missing a description.
- ⚠ Parameter "new_content" in tool "compile_preflight" is missing a description.
- ⚠ Tool "compile_preflight" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "compile_preflight" description is 252 characters. Cline truncates at 200.
- ✖ Tool "compile_preflight" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `configure_objects`

- ⚠ Parameter "config" in tool "configure_objects" is missing a description.
- ⚠ Tool "configure_objects" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "configure_objects" description is 208 characters. Cline truncates at 200.
- ✖ Tool "configure_objects" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `console_mark`

- ⚠ Parameter "label" in tool "console_mark" is missing a description.
- ⚠ Parameter "label" in tool "console_mark" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Tool "console_mark" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "console_mark" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `create_object`

- ⚠ Parameter "name" in tool "create_object" is missing a description.
- ⚠ Parameter "parent" in tool "create_object" is missing a description.
- ⚠ Parameter "components" in tool "create_object" is missing a description.
- ⚠ Parameter "primitive" in tool "create_object" is missing a description.
- ⚠ Parameter "prefab_path" in tool "create_object" is missing a description.
- ⚠ Parameter "scene" in tool "create_object" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.parent" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.components" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.primitive" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.prefab_path" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.scene" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "parent" in tool "create_object" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "components" in tool "create_object" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "primitive" in tool "create_object" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "prefab_path" in tool "create_object" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "scene" in tool "create_object" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.parent" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.components" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.primitive" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.prefab_path" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.scene" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "create_object" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "create_object" description is 231 characters. Cline truncates at 200.
- ✖ Tool "create_object" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `create_ui`

- ⚠ Parameter "type" in tool "create_ui" is missing a description.
- ⚠ Parameter "name" in tool "create_ui" is missing a description.
- ⚠ Parameter "parent" in tool "create_ui" is missing a description.
- ⚠ Parameter "anchor" in tool "create_ui" is missing a description.
- ⚠ Parameter "pos" in tool "create_ui" is missing a description.
- ⚠ Parameter "size" in tool "create_ui" is missing a description.
- ⚠ Parameter "pivot" in tool "create_ui" is missing a description.
- ⚠ Parameter "color" in tool "create_ui" is missing a description.
- ⚠ Parameter "text" in tool "create_ui" is missing a description.
- ⚠ Parameter "font_size" in tool "create_ui" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.name" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.parent" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.anchor" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.pos" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.size" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.pivot" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.color" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.text" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.font_size" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "name" in tool "create_ui" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "parent" in tool "create_ui" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "anchor" in tool "create_ui" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "pos" in tool "create_ui" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "size" in tool "create_ui" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "pivot" in tool "create_ui" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "color" in tool "create_ui" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "text" in tool "create_ui" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "font_size" in tool "create_ui" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.name" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.parent" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.anchor" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.pos" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.size" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.pivot" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.color" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.text" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.font_size" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "create_ui" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "create_ui" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `debug`

- ⚠ Parameter "symptom" in tool "debug" is missing a description.
- ⚠ Parameter "path" in tool "debug" is missing a description.
- ⚠ Parameter "gather" in tool "debug" is missing a description.
- ⚠ Parameter "symptom" in tool "debug" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "path" in tool "debug" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "gather" in tool "debug" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Tool "debug" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "debug" description is 464 characters. Cline truncates at 200.
- ✖ Tool "debug" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `debug_animator`

- ⚠ Parameter "path" in tool "debug_animator" is missing a description.
- ⚠ Tool "debug_animator" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "debug_animator" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `debug_physics`

- ⚠ Parameter "path" in tool "debug_physics" is missing a description.
- ⚠ Parameter "radius" in tool "debug_physics" is missing a description.
- ⚠ Parameter "radius" in tool "debug_physics" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Tool "debug_physics" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "debug_physics" description is 227 characters. Cline truncates at 200.
- ✖ Tool "debug_physics" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `delete_object`

- ⚠ Parameter "id" in tool "delete_object" is missing a description.
- ⚠ Parameter "path" in tool "delete_object" is missing a description.
- ⚠ Parameter "force" in tool "delete_object" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.id" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.path" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "id" in tool "delete_object" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "path" in tool "delete_object" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "force" in tool "delete_object" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.id" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.path" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "delete_object" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "delete_object" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.
- ✖ Schema at "inputSchema.properties.id.anyOf[0]" uses type "integer" which is not allowed by OpenAI Agents SDK. Use one of: string, number, boolean, object, array, null.

#### `diagnose`

- ⚠ Parameter "prev_mvid" in tool "diagnose" is missing a description.
- ⚠ Parameter "expected_compile" in tool "diagnose" is missing a description.
- ⚠ Parameter "prev_mvid" in tool "diagnose" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "expected_compile" in tool "diagnose" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Tool "diagnose" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "diagnose" description is 779 characters. Cline truncates at 200.
- ✖ Tool "diagnose" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `discover_tools`

- ⚠ Parameter "category" in tool "discover_tools" is missing a description.
- ⚠ Parameter "enable" in tool "discover_tools" is missing a description.
- ⚠ Parameter "include_legacy" in tool "discover_tools" is missing a description.
- ⚠ Parameter "structured" in tool "discover_tools" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.category" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "category" in tool "discover_tools" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "enable" in tool "discover_tools" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "include_legacy" in tool "discover_tools" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "structured" in tool "discover_tools" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.category" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "discover_tools" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "discover_tools" description is 267 characters. Cline truncates at 200.
- ✖ Tool "discover_tools" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `do`

- ⚠ Parameter "intent" in tool "do" is missing a description.
- ⚠ Parameter "dry_run" in tool "do" is missing a description.
- ⚠ Parameter "dry_run" in tool "do" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Tool "do" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "do" description is 315 characters. Cline truncates at 200.
- ✖ Tool "do" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `doctor`

- ⚠ Parameter "fix" in tool "doctor" is missing a description.
- ⚠ Parameter "fix" in tool "doctor" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Tool "doctor" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "doctor" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `editor`

- ⚠ Parameter "action" in tool "editor" is missing a description.
- ⚠ Parameter "path" in tool "editor" is missing a description.
- ⚠ Parameter "paths" in tool "editor" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.path" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.paths" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "action" in tool "editor" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "path" in tool "editor" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "paths" in tool "editor" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.path" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.paths" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "editor" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "editor" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `execute_code`

- ⚠ Parameter "code" in tool "execute_code" is missing a description.
- ⚠ Parameter "undo_label" in tool "execute_code" is missing a description.
- ⚠ Parameter "undo_label" in tool "execute_code" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Tool "execute_code" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "execute_code" description is 279 characters. Cline truncates at 200.
- ✖ Tool "execute_code" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `export_playtest_aliases_to_defs`

- ⚠ Parameter "asset" in tool "export_playtest_aliases_to_defs" is missing a description.
- ⚠ Parameter "defs" in tool "export_playtest_aliases_to_defs" is missing a description.
- ⚠ Parameter "asset" in tool "export_playtest_aliases_to_defs" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "defs" in tool "export_playtest_aliases_to_defs" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Tool "export_playtest_aliases_to_defs" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "export_playtest_aliases_to_defs" description is 240 characters. Cline truncates at 200.
- ✖ Tool "export_playtest_aliases_to_defs" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `find_objects`

- ⚠ Parameter "name" in tool "find_objects" is missing a description.
- ⚠ Parameter "tag" in tool "find_objects" is missing a description.
- ⚠ Parameter "layer" in tool "find_objects" is missing a description.
- ⚠ Parameter "component" in tool "find_objects" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.name" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.tag" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.layer" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.component" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "name" in tool "find_objects" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "tag" in tool "find_objects" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "layer" in tool "find_objects" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "component" in tool "find_objects" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.name" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.tag" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.layer" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.component" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "find_objects" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "find_objects" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `fingerprint`

- ⚠ Parameter "path" in tool "fingerprint" is missing a description.
- ⚠ Parameter "depth" in tool "fingerprint" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.path" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "path" in tool "fingerprint" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "depth" in tool "fingerprint" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.path" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "fingerprint" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "fingerprint" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.
- ✖ Schema at "inputSchema.properties.depth" uses type "integer" which is not allowed by OpenAI Agents SDK. Use one of: string, number, boolean, object, array, null.

#### `get_capabilities`

- ⚠ Tool "get_capabilities" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "get_capabilities" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `get_changes`

- ⚠ Parameter "clear" in tool "get_changes" is missing a description.
- ⚠ Parameter "clear" in tool "get_changes" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Tool "get_changes" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "get_changes" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `get_compile_errors`

- ⚠ Tool "get_compile_errors" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "get_compile_errors" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `get_component`

- ⚠ Parameter "path" in tool "get_component" is missing a description.
- ⚠ Parameter "type" in tool "get_component" is missing a description.
- ⚠ Parameter "fields" in tool "get_component" is missing a description.
- ⚠ Parameter "full" in tool "get_component" is missing a description.
- ⚠ Parameter "compress" in tool "get_component" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.fields" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "fields" in tool "get_component" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "full" in tool "get_component" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "compress" in tool "get_component" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.fields" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "get_component" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "get_component" description is 437 characters. Cline truncates at 200.
- ✖ Tool "get_component" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `get_components_list`

- ⚠ Parameter "id" in tool "get_components_list" is missing a description.
- ⚠ Tool "get_components_list" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "get_components_list" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.
- ✖ Schema at "inputSchema.properties.id" uses type "integer" which is not allowed by OpenAI Agents SDK. Use one of: string, number, boolean, object, array, null.

#### `get_console`

- ⚠ Parameter "count" in tool "get_console" is missing a description.
- ⚠ Parameter "level" in tool "get_console" is missing a description.
- ⚠ Parameter "first" in tool "get_console" is missing a description.
- ⚠ Parameter "keyword" in tool "get_console" is missing a description.
- ⚠ Parameter "count_only" in tool "get_console" is missing a description.
- ⚠ Parameter "since" in tool "get_console" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.level" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.keyword" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.since" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "count" in tool "get_console" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "level" in tool "get_console" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "first" in tool "get_console" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "keyword" in tool "get_console" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "count_only" in tool "get_console" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "since" in tool "get_console" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.level" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.keyword" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.since" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "get_console" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "get_console" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.
- ✖ Schema at "inputSchema.properties.count" uses type "integer" which is not allowed by OpenAI Agents SDK. Use one of: string, number, boolean, object, array, null.
- ✖ Schema at "inputSchema.properties.first" uses type "integer" which is not allowed by OpenAI Agents SDK. Use one of: string, number, boolean, object, array, null.

#### `get_console_since`

- ⚠ Parameter "mark_id" in tool "get_console_since" is missing a description.
- ⚠ Parameter "level" in tool "get_console_since" is missing a description.
- ⚠ Parameter "count" in tool "get_console_since" is missing a description.
- ⚠ Parameter "keyword" in tool "get_console_since" is missing a description.
- ⚠ Parameter "count_only" in tool "get_console_since" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.level" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.keyword" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "level" in tool "get_console_since" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "count" in tool "get_console_since" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "keyword" in tool "get_console_since" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "count_only" in tool "get_console_since" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.level" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.keyword" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "get_console_since" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "get_console_since" description is 324 characters. Cline truncates at 200.
- ✖ Tool "get_console_since" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.
- ✖ Schema at "inputSchema.properties.count" uses type "integer" which is not allowed by OpenAI Agents SDK. Use one of: string, number, boolean, object, array, null.

#### `get_enabled_tools`

- ⚠ Tool "get_enabled_tools" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "get_enabled_tools" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `get_frame_stats`

- ⚠ Parameter "include" in tool "get_frame_stats" is missing a description.
- ⚠ Parameter "include" in tool "get_frame_stats" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Tool "get_frame_stats" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "get_frame_stats" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `get_hierarchy`

- ⚠ Parameter "depth" in tool "get_hierarchy" is missing a description.
- ⚠ Parameter "root" in tool "get_hierarchy" is missing a description.
- ⚠ Parameter "filter" in tool "get_hierarchy" is missing a description.
- ⚠ Parameter "components" in tool "get_hierarchy" is missing a description.
- ⚠ Parameter "compress" in tool "get_hierarchy" is missing a description.
- ⚠ Parameter "summary" in tool "get_hierarchy" is missing a description.
- ⚠ Parameter "incremental" in tool "get_hierarchy" is missing a description.
- ⚠ Parameter "full" in tool "get_hierarchy" is missing a description.
- ⚠ Parameter "scene" in tool "get_hierarchy" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.root" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.filter" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.scene" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "depth" in tool "get_hierarchy" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "root" in tool "get_hierarchy" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "filter" in tool "get_hierarchy" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "components" in tool "get_hierarchy" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "compress" in tool "get_hierarchy" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "summary" in tool "get_hierarchy" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "incremental" in tool "get_hierarchy" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "full" in tool "get_hierarchy" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "scene" in tool "get_hierarchy" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.root" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.filter" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.scene" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "get_hierarchy" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "get_hierarchy" description is 462 characters. Cline truncates at 200.
- ✖ Tool "get_hierarchy" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.
- ✖ Schema at "inputSchema.properties.depth" uses type "integer" which is not allowed by OpenAI Agents SDK. Use one of: string, number, boolean, object, array, null.

#### `get_memory`

- ⚠ Parameter "include" in tool "get_memory" is missing a description.
- ⚠ Parameter "include" in tool "get_memory" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Tool "get_memory" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "get_memory" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `get_metrics`

- ⚠ Parameter "format" in tool "get_metrics" is missing a description.
- ⚠ Parameter "reset" in tool "get_metrics" is missing a description.
- ⚠ Parameter "format" in tool "get_metrics" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "reset" in tool "get_metrics" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Tool "get_metrics" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "get_metrics" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `get_object_detail`

- ⚠ Parameter "id" in tool "get_object_detail" is missing a description.
- ⚠ Parameter "full" in tool "get_object_detail" is missing a description.
- ⚠ Parameter "full" in tool "get_object_detail" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Tool "get_object_detail" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "get_object_detail" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.
- ✖ Schema at "inputSchema.properties.id" uses type "integer" which is not allowed by OpenAI Agents SDK. Use one of: string, number, boolean, object, array, null.

#### `get_schema`

- ⚠ Parameter "type" in tool "get_schema" is missing a description.
- ⚠ Tool "get_schema" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "get_schema" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `get_selection`

- ⚠ Tool "get_selection" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "get_selection" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `get_spatial_context`

- ⚠ Parameter "path" in tool "get_spatial_context" is missing a description.
- ⚠ Parameter "radius" in tool "get_spatial_context" is missing a description.
- ⚠ Parameter "radius" in tool "get_spatial_context" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Tool "get_spatial_context" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "get_spatial_context" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `get_test_count`

- ⚠ Tool "get_test_count" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "get_test_count" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `get_test_progress`

- ⚠ Parameter "run_id" in tool "get_test_progress" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.run_id" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "run_id" in tool "get_test_progress" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.run_id" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "get_test_progress" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "get_test_progress" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `get_test_results`

- ⚠ Parameter "run_id" in tool "get_test_results" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.run_id" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "run_id" in tool "get_test_results" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.run_id" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "get_test_results" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "get_test_results" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `get_test_run`

- ⚠ Parameter "run_id" in tool "get_test_run" is missing a description.
- ⚠ Tool "get_test_run" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "get_test_run" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `get_unity_events`

- ⚠ Parameter "path" in tool "get_unity_events" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.path" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "path" in tool "get_unity_events" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.path" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "get_unity_events" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "get_unity_events" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `get_watches`

- ⚠ Tool "get_watches" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "get_watches" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `inspect`

- ⚠ Parameter "paths" in tool "inspect" is missing a description.
- ⚠ Parameter "components" in tool "inspect" is missing a description.
- ⚠ Parameter "fields" in tool "inspect" is missing a description.
- ⚠ Parameter "full" in tool "inspect" is missing a description.
- ⚠ Parameter "compress" in tool "inspect" is missing a description.
- ⚠ Parameter "find_type" in tool "inspect" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.paths" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.components" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.fields" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.find_type" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "paths" in tool "inspect" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "components" in tool "inspect" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "fields" in tool "inspect" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "full" in tool "inspect" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "compress" in tool "inspect" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "find_type" in tool "inspect" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.paths" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.components" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.fields" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.find_type" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "inspect" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "inspect" description is 405 characters. Cline truncates at 200.
- ✖ Tool "inspect" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `invoke_method`

- ⚠ Parameter "path" in tool "invoke_method" is missing a description.
- ⚠ Parameter "component" in tool "invoke_method" is missing a description.
- ⚠ Parameter "method" in tool "invoke_method" is missing a description.
- ⚠ Parameter "args" in tool "invoke_method" is missing a description.
- ⚠ Parameter "args" in tool "invoke_method" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Tool "invoke_method" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "invoke_method" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `lint_playtest`

- ⚠ Parameter "path" in tool "lint_playtest" is missing a description.
- ⚠ Parameter "script" in tool "lint_playtest" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.path" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.script" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "path" in tool "lint_playtest" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "script" in tool "lint_playtest" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.path" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.script" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "lint_playtest" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "lint_playtest" description is 427 characters. Cline truncates at 200.
- ✖ Tool "lint_playtest" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `lint_playtest_suite`

- ⚠ Parameter "pattern" in tool "lint_playtest_suite" is missing a description.
- ⚠ Parameter "suite_path" in tool "lint_playtest_suite" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.pattern" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.suite_path" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "pattern" in tool "lint_playtest_suite" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "suite_path" in tool "lint_playtest_suite" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.pattern" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.suite_path" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "lint_playtest_suite" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "lint_playtest_suite" description is 300 characters. Cline truncates at 200.
- ✖ Tool "lint_playtest_suite" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `lint_scene_refs`

- ⚠ Parameter "path" in tool "lint_scene_refs" is missing a description.
- ⚠ Parameter "snippet" in tool "lint_scene_refs" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.path" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.snippet" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "path" in tool "lint_scene_refs" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "snippet" in tool "lint_scene_refs" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.path" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.snippet" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "lint_scene_refs" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "lint_scene_refs" description is 378 characters. Cline truncates at 200.
- ✖ Tool "lint_scene_refs" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `list_connections`

- ⚠ Tool "list_connections" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "list_connections" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `list_skills`

- ⚠ Tool "list_skills" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "list_skills" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `list_templates`

- ⚠ Tool "list_templates" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "list_templates" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `list_test_runs`

- ⚠ Parameter "limit" in tool "list_test_runs" is missing a description.
- ⚠ Parameter "limit" in tool "list_test_runs" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Tool "list_test_runs" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "list_test_runs" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.
- ✖ Schema at "inputSchema.properties.limit" uses type "integer" which is not allowed by OpenAI Agents SDK. Use one of: string, number, boolean, object, array, null.

#### `load_session`

- ⚠ Tool "load_session" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "load_session" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `manage_component`

- ⚠ Parameter "path" in tool "manage_component" is missing a description.
- ⚠ Parameter "type" in tool "manage_component" is missing a description.
- ⚠ Parameter "action" in tool "manage_component" is missing a description.
- ⚠ Tool "manage_component" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "manage_component" description is 215 characters. Cline truncates at 200.
- ✖ Tool "manage_component" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `material`

- ⚠ Parameter "action" in tool "material" is missing a description.
- ⚠ Parameter "path" in tool "material" is missing a description.
- ⚠ Parameter "object_path" in tool "material" is missing a description.
- ⚠ Parameter "shader" in tool "material" is missing a description.
- ⚠ Parameter "prop" in tool "material" is missing a description.
- ⚠ Parameter "value" in tool "material" is missing a description.
- ⚠ Parameter "source" in tool "material" is missing a description.
- ⚠ Parameter "targets" in tool "material" is missing a description.
- ⚠ Parameter "slot" in tool "material" is missing a description.
- ⚠ Parameter "filter" in tool "material" is missing a description.
- ⚠ Parameter "target" in tool "material" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.path" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.object_path" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.shader" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.prop" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.value" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.source" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.targets" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.slot" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.filter" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.target" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "path" in tool "material" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "object_path" in tool "material" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "shader" in tool "material" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "prop" in tool "material" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "value" in tool "material" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "source" in tool "material" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "targets" in tool "material" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "slot" in tool "material" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "filter" in tool "material" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "target" in tool "material" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.path" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.object_path" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.shader" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.prop" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.value" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.source" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.targets" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.slot" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.filter" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.target" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "material" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "material" description is 521 characters. Cline truncates at 200.
- ✖ Tool "material" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.
- ✖ Schema at "inputSchema.properties.slot.anyOf[0]" uses type "integer" which is not allowed by OpenAI Agents SDK. Use one of: string, number, boolean, object, array, null.

#### `material_audit`

- ⚠ Parameter "action" in tool "material_audit" is missing a description.
- ⚠ Parameter "platform" in tool "material_audit" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.platform" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "action" in tool "material_audit" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "platform" in tool "material_audit" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.platform" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "material_audit" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "material_audit" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `mcp_status`

- ⚠ Tool "mcp_status" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "mcp_status" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `menu`

- ⚠ Parameter "action" in tool "menu" is missing a description.
- ⚠ Parameter "path" in tool "menu" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.path" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "path" in tool "menu" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.path" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "menu" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "menu" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `move_to`

- ⚠ Parameter "path" in tool "move_to" is missing a description.
- ⚠ Parameter "position" in tool "move_to" is missing a description.
- ⚠ Parameter "timeout" in tool "move_to" is missing a description.
- ⚠ Parameter "timeout" in tool "move_to" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Tool "move_to" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "move_to" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `navmesh_query`

- ⚠ Parameter "action" in tool "navmesh_query" is missing a description.
- ⚠ Parameter "center" in tool "navmesh_query" is missing a description.
- ⚠ Parameter "from_pos" in tool "navmesh_query" is missing a description.
- ⚠ Parameter "to" in tool "navmesh_query" is missing a description.
- ⚠ Parameter "max_distance" in tool "navmesh_query" is missing a description.
- ⚠ Parameter "area_mask" in tool "navmesh_query" is missing a description.
- ⚠ Parameter "agentRadius" in tool "navmesh_query" is missing a description.
- ⚠ Parameter "agentHeight" in tool "navmesh_query" is missing a description.
- ⚠ Parameter "agentClimb" in tool "navmesh_query" is missing a description.
- ⚠ Parameter "agentSlope" in tool "navmesh_query" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.center" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.from_pos" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.to" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.agentRadius" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.agentHeight" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.agentClimb" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.agentSlope" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "center" in tool "navmesh_query" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "from_pos" in tool "navmesh_query" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "to" in tool "navmesh_query" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "max_distance" in tool "navmesh_query" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "area_mask" in tool "navmesh_query" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "agentRadius" in tool "navmesh_query" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "agentHeight" in tool "navmesh_query" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "agentClimb" in tool "navmesh_query" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "agentSlope" in tool "navmesh_query" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.center" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.from_pos" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.to" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.agentRadius" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.agentHeight" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.agentClimb" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.agentSlope" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "navmesh_query" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "navmesh_query" description is 595 characters. Cline truncates at 200.
- ✖ Tool "navmesh_query" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.
- ✖ Schema at "inputSchema.properties.area_mask" uses type "integer" which is not allowed by OpenAI Agents SDK. Use one of: string, number, boolean, object, array, null.

#### `object_diff`

- ⚠ Parameter "path_a" in tool "object_diff" is missing a description.
- ⚠ Parameter "path_b" in tool "object_diff" is missing a description.
- ⚠ Tool "object_diff" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "object_diff" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `package`

- ⚠ Parameter "action" in tool "package" is missing a description.
- ⚠ Parameter "name" in tool "package" is missing a description.
- ⚠ Parameter "version" in tool "package" is missing a description.
- ⚠ Parameter "query" in tool "package" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.name" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.version" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.query" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "name" in tool "package" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "version" in tool "package" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "query" in tool "package" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.name" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.version" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.query" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "package" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "package" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `particle`

- ⚠ Parameter "action" in tool "particle" is missing a description.
- ⚠ Parameter "path" in tool "particle" is missing a description.
- ⚠ Parameter "name" in tool "particle" is missing a description.
- ⚠ Parameter "module" in tool "particle" is missing a description.
- ⚠ Parameter "prop" in tool "particle" is missing a description.
- ⚠ Parameter "value" in tool "particle" is missing a description.
- ⚠ Parameter "preset" in tool "particle" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.name" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.module" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.prop" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.value" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.preset" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "name" in tool "particle" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "module" in tool "particle" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "prop" in tool "particle" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "value" in tool "particle" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "preset" in tool "particle" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.name" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.module" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.prop" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.value" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.preset" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "particle" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "particle" description is 269 characters. Cline truncates at 200.
- ✖ Tool "particle" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `permission_prompt`

- ⚠ Parameter "tool_name" in tool "permission_prompt" is missing a description.
- ⚠ Parameter "input" in tool "permission_prompt" is missing a description.
- ⚠ Parameter "tool_use_id" in tool "permission_prompt" is missing a description.
- ⚠ Tool "permission_prompt" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "permission_prompt" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `ping_object`

- ⚠ Parameter "path" in tool "ping_object" is missing a description.
- ⚠ Tool "ping_object" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "ping_object" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `prefab`

- ⚠ Parameter "action" in tool "prefab" is missing a description.
- ⚠ Parameter "path" in tool "prefab" is missing a description.
- ⚠ Parameter "asset_path" in tool "prefab" is missing a description.
- ⚠ Parameter "base_path" in tool "prefab" is missing a description.
- ⚠ Parameter "variant_path" in tool "prefab" is missing a description.
- ⚠ Parameter "component" in tool "prefab" is missing a description.
- ⚠ Parameter "prop" in tool "prefab" is missing a description.
- ⚠ Parameter "value" in tool "prefab" is missing a description.
- ⚠ Parameter "add_component" in tool "prefab" is missing a description.
- ⚠ Parameter "remove_component" in tool "prefab" is missing a description.
- ⚠ Parameter "recursive" in tool "prefab" is missing a description.
- ⚠ Parameter "mode" in tool "prefab" is missing a description.
- ⚠ Parameter "scope" in tool "prefab" is missing a description.
- ⚠ Parameter "format" in tool "prefab" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.path" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.asset_path" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.base_path" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.variant_path" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.component" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.prop" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.value" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.add_component" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.remove_component" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.mode" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.scope" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.format" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "path" in tool "prefab" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "asset_path" in tool "prefab" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "base_path" in tool "prefab" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "variant_path" in tool "prefab" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "component" in tool "prefab" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "prop" in tool "prefab" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "value" in tool "prefab" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "add_component" in tool "prefab" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "remove_component" in tool "prefab" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "recursive" in tool "prefab" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "mode" in tool "prefab" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "scope" in tool "prefab" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "format" in tool "prefab" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.path" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.asset_path" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.base_path" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.variant_path" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.component" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.prop" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.value" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.add_component" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.remove_component" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.mode" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.scope" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.format" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "prefab" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "prefab" description is 530 characters. Cline truncates at 200.
- ✖ Tool "prefab" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `profile`

- ⚠ Parameter "action" in tool "profile" is missing a description.
- ⚠ Parameter "duration" in tool "profile" is missing a description.
- ⚠ Parameter "session" in tool "profile" is missing a description.
- ⚠ Parameter "compare_with" in tool "profile" is missing a description.
- ⚠ Parameter "focus" in tool "profile" is missing a description.
- ⚠ Parameter "mode" in tool "profile" is missing a description.
- ⚠ Parameter "threshold_ms" in tool "profile" is missing a description.
- ⚠ Parameter "duration" in tool "profile" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "session" in tool "profile" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "compare_with" in tool "profile" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "focus" in tool "profile" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "mode" in tool "profile" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "threshold_ms" in tool "profile" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Tool "profile" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "profile" description is 245 characters. Cline truncates at 200.
- ✖ Tool "profile" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `project_settings`

- ⚠ Parameter "action" in tool "project_settings" is missing a description.
- ⚠ Parameter "target" in tool "project_settings" is missing a description.
- ⚠ Parameter "prop" in tool "project_settings" is missing a description.
- ⚠ Parameter "value" in tool "project_settings" is missing a description.
- ⚠ Parameter "index" in tool "project_settings" is missing a description.
- ⚠ Parameter "build_target" in tool "project_settings" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.prop" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.value" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.index" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.build_target" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "prop" in tool "project_settings" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "value" in tool "project_settings" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "index" in tool "project_settings" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "build_target" in tool "project_settings" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.prop" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.value" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.index" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.build_target" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "project_settings" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "project_settings" description is 350 characters. Cline truncates at 200.
- ✖ Tool "project_settings" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.
- ✖ Schema at "inputSchema.properties.index.anyOf[0]" uses type "integer" which is not allowed by OpenAI Agents SDK. Use one of: string, number, boolean, object, array, null.

#### `query_state`

- ⚠ Parameter "queries" in tool "query_state" is missing a description.
- ⚠ Tool "query_state" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "query_state" description is 211 characters. Cline truncates at 200.
- ✖ Tool "query_state" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `recompile`

- ⚠ Tool "recompile" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "recompile" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `reconnect_unity`

- ⚠ Parameter "port" in tool "reconnect_unity" is missing a description.
- ⚠ Parameter "port" in tool "reconnect_unity" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Tool "reconnect_unity" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "reconnect_unity" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.
- ✖ Schema at "inputSchema.properties.port" uses type "integer" which is not allowed by OpenAI Agents SDK. Use one of: string, number, boolean, object, array, null.

#### `references`

- ⚠ Parameter "action" in tool "references" is missing a description.
- ⚠ Parameter "path" in tool "references" is missing a description.
- ⚠ Parameter "children" in tool "references" is missing a description.
- ⚠ Parameter "depth" in tool "references" is missing a description.
- ⚠ Parameter "source" in tool "references" is missing a description.
- ⚠ Parameter "target" in tool "references" is missing a description.
- ⚠ Parameter "mappings" in tool "references" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.source" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.target" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.mappings" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "children" in tool "references" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "depth" in tool "references" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "source" in tool "references" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "target" in tool "references" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "mappings" in tool "references" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.source" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.target" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.mappings" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "references" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "references" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.
- ✖ Schema at "inputSchema.properties.depth" uses type "integer" which is not allowed by OpenAI Agents SDK. Use one of: string, number, boolean, object, array, null.

#### `region_clear`

- ⚠ Parameter "vertices" in tool "region_clear" is missing a description.
- ⚠ Parameter "dry_run" in tool "region_clear" is missing a description.
- ⚠ Parameter "filter" in tool "region_clear" is missing a description.
- ⚠ Parameter "cap" in tool "region_clear" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.filter" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "dry_run" in tool "region_clear" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "filter" in tool "region_clear" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "cap" in tool "region_clear" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.filter" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "region_clear" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "region_clear" description is 371 characters. Cline truncates at 200.
- ✖ Tool "region_clear" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.
- ✖ Schema at "inputSchema.properties.cap" uses type "integer" which is not allowed by OpenAI Agents SDK. Use one of: string, number, boolean, object, array, null.

#### `release_smoke`

- ⚠ Tool "release_smoke" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "release_smoke" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `rename_object`

- ⚠ Parameter "path" in tool "rename_object" is missing a description.
- ⚠ Parameter "name" in tool "rename_object" is missing a description.
- ⚠ Tool "rename_object" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "rename_object" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `render_analyze`

- ⚠ Parameter "action" in tool "render_analyze" is missing a description.
- ⚠ Parameter "path" in tool "render_analyze" is missing a description.
- ⚠ Parameter "detail" in tool "render_analyze" is missing a description.
- ⚠ Parameter "baseline_id" in tool "render_analyze" is missing a description.
- ⚠ Parameter "max_events" in tool "render_analyze" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.path" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.baseline_id" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.max_events" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "path" in tool "render_analyze" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "detail" in tool "render_analyze" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "baseline_id" in tool "render_analyze" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "max_events" in tool "render_analyze" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.path" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.baseline_id" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.max_events" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "render_analyze" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "render_analyze" description is 573 characters. Cline truncates at 200.
- ✖ Tool "render_analyze" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.
- ✖ Schema at "inputSchema.properties.max_events.anyOf[0]" uses type "integer" which is not allowed by OpenAI Agents SDK. Use one of: string, number, boolean, object, array, null.

#### `resolve_scene_refs`

- ⚠ Parameter "refs" in tool "resolve_scene_refs" is missing a description.
- ⚠ Parameter "fields" in tool "resolve_scene_refs" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.fields" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "fields" in tool "resolve_scene_refs" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.fields" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "resolve_scene_refs" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "resolve_scene_refs" description is 264 characters. Cline truncates at 200.
- ✖ Tool "resolve_scene_refs" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `resolve_test_request`

- ⚠ Parameter "request_id" in tool "resolve_test_request" is missing a description.
- ⚠ Tool "resolve_test_request" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "resolve_test_request" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `resolve_tool_schema`

- ⚠ Parameter "tools" in tool "resolve_tool_schema" is missing a description.
- ⚠ Tool "resolve_tool_schema" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "resolve_tool_schema" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `run_playtest`

- ⚠ Parameter "script" in tool "run_playtest" is missing a description.
- ⚠ Parameter "timeout" in tool "run_playtest" is missing a description.
- ⚠ Parameter "abort_on_fail" in tool "run_playtest" is missing a description.
- ⚠ Parameter "defs" in tool "run_playtest" is missing a description.
- ⚠ Parameter "path" in tool "run_playtest" is missing a description.
- ⚠ Parameter "snapshot_on_failure" in tool "run_playtest" is missing a description.
- ⚠ Parameter "fresh" in tool "run_playtest" is missing a description.
- ⚠ Parameter "before_hook" in tool "run_playtest" is missing a description.
- ⚠ Parameter "after_hook" in tool "run_playtest" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.script" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.defs" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.path" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.before_hook" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.after_hook" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "script" in tool "run_playtest" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "timeout" in tool "run_playtest" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "abort_on_fail" in tool "run_playtest" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "defs" in tool "run_playtest" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "path" in tool "run_playtest" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "snapshot_on_failure" in tool "run_playtest" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "fresh" in tool "run_playtest" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "before_hook" in tool "run_playtest" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "after_hook" in tool "run_playtest" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.script" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.defs" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.path" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.before_hook" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.after_hook" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "run_playtest" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "run_playtest" description is 752 characters. Cline truncates at 200.
- ✖ Tool "run_playtest" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `run_playtest_suite`

- ⚠ Parameter "pattern" in tool "run_playtest_suite" is missing a description.
- ⚠ Parameter "suite_path" in tool "run_playtest_suite" is missing a description.
- ⚠ Parameter "timeout_per_test" in tool "run_playtest_suite" is missing a description.
- ⚠ Parameter "stop_on_fail" in tool "run_playtest_suite" is missing a description.
- ⚠ Parameter "stop_after" in tool "run_playtest_suite" is missing a description.
- ⚠ Parameter "auto_play" in tool "run_playtest_suite" is missing a description.
- ⚠ Parameter "restart_between" in tool "run_playtest_suite" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.pattern" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.suite_path" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "pattern" in tool "run_playtest_suite" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "suite_path" in tool "run_playtest_suite" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "timeout_per_test" in tool "run_playtest_suite" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "stop_on_fail" in tool "run_playtest_suite" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "stop_after" in tool "run_playtest_suite" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "auto_play" in tool "run_playtest_suite" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "restart_between" in tool "run_playtest_suite" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.pattern" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.suite_path" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "run_playtest_suite" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "run_playtest_suite" description is 731 characters. Cline truncates at 200.
- ✖ Tool "run_playtest_suite" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `run_tests`

- ⚠ Parameter "mode" in tool "run_tests" is missing a description.
- ⚠ Parameter "filter" in tool "run_tests" is missing a description.
- ⚠ Parameter "request_id" in tool "run_tests" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.filter" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.request_id" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "mode" in tool "run_tests" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "filter" in tool "run_tests" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "request_id" in tool "run_tests" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.filter" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.request_id" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "run_tests" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "run_tests" description is 356 characters. Cline truncates at 200.
- ✖ Tool "run_tests" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `run_tests_wait`

- ⚠ Parameter "mode" in tool "run_tests_wait" is missing a description.
- ⚠ Parameter "filter" in tool "run_tests_wait" is missing a description.
- ⚠ Parameter "timeout" in tool "run_tests_wait" is missing a description.
- ⚠ Parameter "poll_interval" in tool "run_tests_wait" is missing a description.
- ⚠ Parameter "request_id" in tool "run_tests_wait" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.request_id" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "mode" in tool "run_tests_wait" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "filter" in tool "run_tests_wait" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "timeout" in tool "run_tests_wait" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "poll_interval" in tool "run_tests_wait" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "request_id" in tool "run_tests_wait" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.request_id" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "run_tests_wait" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "run_tests_wait" description is 288 characters. Cline truncates at 200.
- ✖ Tool "run_tests_wait" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `runtime_snapshot`

- ⚠ Parameter "type" in tool "runtime_snapshot" is missing a description.
- ⚠ Parameter "name" in tool "runtime_snapshot" is missing a description.
- ⚠ Parameter "component" in tool "runtime_snapshot" is missing a description.
- ⚠ Parameter "compress" in tool "runtime_snapshot" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.name" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.component" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "name" in tool "runtime_snapshot" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "component" in tool "runtime_snapshot" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "compress" in tool "runtime_snapshot" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.name" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.component" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "runtime_snapshot" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "runtime_snapshot" description is 326 characters. Cline truncates at 200.
- ✖ Tool "runtime_snapshot" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `save_session`

- ⚠ Tool "save_session" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "save_session" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `save_skill`

- ⚠ Parameter "name" in tool "save_skill" is missing a description.
- ⚠ Parameter "description" in tool "save_skill" is missing a description.
- ⚠ Parameter "code" in tool "save_skill" is missing a description.
- ⚠ Tool "save_skill" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "save_skill" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `save_template`

- ⚠ Parameter "name" in tool "save_template" is missing a description.
- ⚠ Parameter "code" in tool "save_template" is missing a description.
- ⚠ Tool "save_template" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "save_template" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `scan_scene`

- ⚠ Tool "scan_scene" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "scan_scene" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `scene`

- ⚠ Parameter "action" in tool "scene" is missing a description.
- ⚠ Parameter "path" in tool "scene" is missing a description.
- ⚠ Parameter "scene" in tool "scene" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.path" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.scene" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "path" in tool "scene" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "scene" in tool "scene" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.path" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.scene" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "scene" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "scene" description is 253 characters. Cline truncates at 200.
- ✖ Tool "scene" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `scene_change_plan`

- ⚠ Parameter "goal" in tool "scene_change_plan" is missing a description.
- ⚠ Parameter "targets" in tool "scene_change_plan" is missing a description.
- ⚠ Parameter "dry_run" in tool "scene_change_plan" is missing a description.
- ⚠ Parameter "targets" in tool "scene_change_plan" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "dry_run" in tool "scene_change_plan" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Tool "scene_change_plan" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "scene_change_plan" description is 269 characters. Cline truncates at 200.
- ✖ Tool "scene_change_plan" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `scene_diff`

- ⚠ Tool "scene_diff" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "scene_diff" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `scene_environment`

- ⚠ Parameter "action" in tool "scene_environment" is missing a description.
- ⚠ Parameter "prop" in tool "scene_environment" is missing a description.
- ⚠ Parameter "value" in tool "scene_environment" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.prop" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.value" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "action" in tool "scene_environment" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "prop" in tool "scene_environment" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "value" in tool "scene_environment" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.prop" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.value" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "scene_environment" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "scene_environment" description is 405 characters. Cline truncates at 200.
- ✖ Tool "scene_environment" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `scene_health`

- ⚠ Parameter "focus" in tool "scene_health" is missing a description.
- ⚠ Parameter "focus" in tool "scene_health" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Tool "scene_health" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "scene_health" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `screenshot`

- ⚠ Parameter "width" in tool "screenshot" is missing a description.
- ⚠ Parameter "height" in tool "screenshot" is missing a description.
- ⚠ Parameter "camera" in tool "screenshot" is missing a description.
- ⚠ Parameter "path" in tool "screenshot" is missing a description.
- ⚠ Parameter "output_path" in tool "screenshot" is missing a description.
- ⚠ Parameter "describe" in tool "screenshot" is missing a description.
- ⚠ Parameter "raw" in tool "screenshot" is missing a description.
- ⚠ Parameter "zoom" in tool "screenshot" is missing a description.
- ⚠ Parameter "angles" in tool "screenshot" is missing a description.
- ⚠ Parameter "supersample" in tool "screenshot" is missing a description.
- ⚠ Parameter "offset" in tool "screenshot" is missing a description.
- ⚠ Parameter "fixed_size" in tool "screenshot" is missing a description.
- ⚠ Parameter "highlight" in tool "screenshot" is missing a description.
- ⚠ Parameter "show_colliders" in tool "screenshot" is missing a description.
- ⚠ Parameter "angle" in tool "screenshot" is missing a description.
- ⚠ Parameter "annotation_id" in tool "screenshot" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.camera" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.path" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.output_path" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.describe" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.zoom" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.angles" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.supersample" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.offset" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.fixed_size" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.highlight" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.show_colliders" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.angle" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.annotation_id" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "width" in tool "screenshot" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "height" in tool "screenshot" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "camera" in tool "screenshot" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "path" in tool "screenshot" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "output_path" in tool "screenshot" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "describe" in tool "screenshot" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "raw" in tool "screenshot" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "zoom" in tool "screenshot" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "angles" in tool "screenshot" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "supersample" in tool "screenshot" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "offset" in tool "screenshot" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "fixed_size" in tool "screenshot" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "highlight" in tool "screenshot" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "show_colliders" in tool "screenshot" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "angle" in tool "screenshot" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "annotation_id" in tool "screenshot" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.camera" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.path" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.output_path" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.describe" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.zoom" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.angles" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.supersample" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.offset" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.fixed_size" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.highlight" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.show_colliders" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.angle" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.annotation_id" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "screenshot" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "screenshot" has 16 parameters. VS Code Copilot performance degrades with more than 15. Consider splitting into multiple tools.
- ⚠ Tool "screenshot" description is 516 characters. Cline truncates at 200.
- ✖ Tool "screenshot" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.
- ✖ Schema at "inputSchema.properties.width" uses type "integer" which is not allowed by OpenAI Agents SDK. Use one of: string, number, boolean, object, array, null.
- ✖ Schema at "inputSchema.properties.height" uses type "integer" which is not allowed by OpenAI Agents SDK. Use one of: string, number, boolean, object, array, null.
- ✖ Schema at "inputSchema.properties.supersample.anyOf[0]" uses type "integer" which is not allowed by OpenAI Agents SDK. Use one of: string, number, boolean, object, array, null.

#### `screenshot_baseline`

- ⚠ Parameter "name" in tool "screenshot_baseline" is missing a description.
- ⚠ Parameter "width" in tool "screenshot_baseline" is missing a description.
- ⚠ Parameter "height" in tool "screenshot_baseline" is missing a description.
- ⚠ Parameter "camera" in tool "screenshot_baseline" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.camera" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "name" in tool "screenshot_baseline" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "width" in tool "screenshot_baseline" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "height" in tool "screenshot_baseline" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "camera" in tool "screenshot_baseline" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.camera" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "screenshot_baseline" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "screenshot_baseline" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.
- ✖ Schema at "inputSchema.properties.width" uses type "integer" which is not allowed by OpenAI Agents SDK. Use one of: string, number, boolean, object, array, null.
- ✖ Schema at "inputSchema.properties.height" uses type "integer" which is not allowed by OpenAI Agents SDK. Use one of: string, number, boolean, object, array, null.

#### `screenshot_compare`

- ⚠ Parameter "name" in tool "screenshot_compare" is missing a description.
- ⚠ Parameter "width" in tool "screenshot_compare" is missing a description.
- ⚠ Parameter "height" in tool "screenshot_compare" is missing a description.
- ⚠ Parameter "camera" in tool "screenshot_compare" is missing a description.
- ⚠ Parameter "mode" in tool "screenshot_compare" is missing a description.
- ⚠ Parameter "question" in tool "screenshot_compare" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.camera" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.question" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "name" in tool "screenshot_compare" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "width" in tool "screenshot_compare" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "height" in tool "screenshot_compare" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "camera" in tool "screenshot_compare" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "mode" in tool "screenshot_compare" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "question" in tool "screenshot_compare" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.camera" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.question" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "screenshot_compare" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "screenshot_compare" description is 265 characters. Cline truncates at 200.
- ✖ Tool "screenshot_compare" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.
- ✖ Schema at "inputSchema.properties.width" uses type "integer" which is not allowed by OpenAI Agents SDK. Use one of: string, number, boolean, object, array, null.
- ✖ Schema at "inputSchema.properties.height" uses type "integer" which is not allowed by OpenAI Agents SDK. Use one of: string, number, boolean, object, array, null.

#### `scriptable_object`

- ⚠ Parameter "action" in tool "scriptable_object" is missing a description.
- ⚠ Parameter "path" in tool "scriptable_object" is missing a description.
- ⚠ Parameter "type" in tool "scriptable_object" is missing a description.
- ⚠ Parameter "prop" in tool "scriptable_object" is missing a description.
- ⚠ Parameter "value" in tool "scriptable_object" is missing a description.
- ⚠ Parameter "fields" in tool "scriptable_object" is missing a description.
- ⚠ Parameter "filter" in tool "scriptable_object" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.path" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.type" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.prop" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.value" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.fields" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.filter" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "path" in tool "scriptable_object" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "type" in tool "scriptable_object" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "prop" in tool "scriptable_object" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "value" in tool "scriptable_object" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "fields" in tool "scriptable_object" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "filter" in tool "scriptable_object" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.path" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.type" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.prop" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.value" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.fields" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.filter" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "scriptable_object" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "scriptable_object" description is 212 characters. Cline truncates at 200.
- ✖ Tool "scriptable_object" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `search_scene`

- ⚠ Parameter "query" in tool "search_scene" is missing a description.
- ⚠ Parameter "root" in tool "search_scene" is missing a description.
- ⚠ Parameter "limit" in tool "search_scene" is missing a description.
- ⚠ Parameter "scene" in tool "search_scene" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.root" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.scene" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "root" in tool "search_scene" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "limit" in tool "search_scene" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "scene" in tool "search_scene" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.root" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.scene" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "search_scene" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "search_scene" description is 285 characters. Cline truncates at 200.
- ✖ Tool "search_scene" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.
- ✖ Schema at "inputSchema.properties.limit" uses type "integer" which is not allowed by OpenAI Agents SDK. Use one of: string, number, boolean, object, array, null.

#### `serialized_field_rename_audit`

- ⚠ Parameter "type" in tool "serialized_field_rename_audit" is missing a description.
- ⚠ Parameter "old_field" in tool "serialized_field_rename_audit" is missing a description.
- ⚠ Parameter "new_field" in tool "serialized_field_rename_audit" is missing a description.
- ⚠ Parameter "include" in tool "serialized_field_rename_audit" is missing a description.
- ⚠ Parameter "include" in tool "serialized_field_rename_audit" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Tool "serialized_field_rename_audit" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "serialized_field_rename_audit" description is 425 characters. Cline truncates at 200.
- ✖ Tool "serialized_field_rename_audit" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `set_active`

- ⚠ Parameter "path" in tool "set_active" is missing a description.
- ⚠ Parameter "active" in tool "set_active" is missing a description.
- ⚠ Tool "set_active" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "set_active" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `set_llm_config`

- ⚠ Parameter "config" in tool "set_llm_config" is missing a description.
- ⚠ Tool "set_llm_config" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "set_llm_config" description is 243 characters. Cline truncates at 200.
- ✖ Tool "set_llm_config" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `set_material`

- ⚠ Parameter "path" in tool "set_material" is missing a description.
- ⚠ Parameter "color" in tool "set_material" is missing a description.
- ⚠ Parameter "shader" in tool "set_material" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.shader" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "shader" in tool "set_material" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.shader" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "set_material" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "set_material" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `set_parent`

- ⚠ Parameter "path" in tool "set_parent" is missing a description.
- ⚠ Parameter "parent" in tool "set_parent" is missing a description.
- ⚠ Parameter "world_position_stays" in tool "set_parent" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.parent" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "parent" in tool "set_parent" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "world_position_stays" in tool "set_parent" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.parent" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "set_parent" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "set_parent" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `set_properties`

- ⚠ Parameter "path" in tool "set_properties" is missing a description.
- ⚠ Parameter "props" in tool "set_properties" is missing a description.
- ⚠ Tool "set_properties" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "set_properties" description is 221 characters. Cline truncates at 200.
- ✖ Tool "set_properties" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `set_property`

- ⚠ Parameter "path" in tool "set_property" is missing a description.
- ⚠ Parameter "component" in tool "set_property" is missing a description.
- ⚠ Parameter "prop" in tool "set_property" is missing a description.
- ⚠ Parameter "value" in tool "set_property" is missing a description.
- ⚠ Parameter "dry_run" in tool "set_property" is missing a description.
- ⚠ Parameter "find_type" in tool "set_property" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.path" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.find_type" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "path" in tool "set_property" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "component" in tool "set_property" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "prop" in tool "set_property" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "value" in tool "set_property" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "dry_run" in tool "set_property" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "find_type" in tool "set_property" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.path" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.find_type" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "set_property" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "set_property" description is 424 characters. Cline truncates at 200.
- ✖ Tool "set_property" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `set_property_delta`

- ⚠ Parameter "path" in tool "set_property_delta" is missing a description.
- ⚠ Parameter "component" in tool "set_property_delta" is missing a description.
- ⚠ Parameter "prop" in tool "set_property_delta" is missing a description.
- ⚠ Parameter "delta" in tool "set_property_delta" is missing a description.
- ⚠ Tool "set_property_delta" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "set_property_delta" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `set_rect`

- ⚠ Parameter "path" in tool "set_rect" is missing a description.
- ⚠ Parameter "anchor" in tool "set_rect" is missing a description.
- ⚠ Parameter "pos" in tool "set_rect" is missing a description.
- ⚠ Parameter "size" in tool "set_rect" is missing a description.
- ⚠ Parameter "pivot" in tool "set_rect" is missing a description.
- ⚠ Parameter "offset_min" in tool "set_rect" is missing a description.
- ⚠ Parameter "offset_max" in tool "set_rect" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.anchor" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.pos" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.size" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.pivot" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.offset_min" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.offset_max" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "anchor" in tool "set_rect" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "pos" in tool "set_rect" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "size" in tool "set_rect" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "pivot" in tool "set_rect" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "offset_min" in tool "set_rect" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "offset_max" in tool "set_rect" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.anchor" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.pos" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.size" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.pivot" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.offset_min" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.offset_max" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "set_rect" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "set_rect" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `set_sibling_index`

- ⚠ Parameter "path" in tool "set_sibling_index" is missing a description.
- ⚠ Parameter "index" in tool "set_sibling_index" is missing a description.
- ⚠ Tool "set_sibling_index" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "set_sibling_index" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.
- ✖ Schema at "inputSchema.properties.index" uses type "integer" which is not allowed by OpenAI Agents SDK. Use one of: string, number, boolean, object, array, null.

#### `setup_objects`

- ⚠ Parameter "specs" in tool "setup_objects" is missing a description.
- ⚠ Tool "setup_objects" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "setup_objects" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `shader`

- ⚠ Parameter "action" in tool "shader" is missing a description.
- ⚠ Parameter "path" in tool "shader" is missing a description.
- ⚠ Parameter "target" in tool "shader" is missing a description.
- ⚠ Parameter "preset" in tool "shader" is missing a description.
- ⚠ Parameter "code" in tool "shader" is missing a description.
- ⚠ Parameter "shader_name" in tool "shader" is missing a description.
- ⚠ Parameter "prop" in tool "shader" is missing a description.
- ⚠ Parameter "value" in tool "shader" is missing a description.
- ⚠ Parameter "keyword" in tool "shader" is missing a description.
- ⚠ Parameter "enabled" in tool "shader" is missing a description.
- ⚠ Parameter "node_type" in tool "shader" is missing a description.
- ⚠ Parameter "node_id" in tool "shader" is missing a description.
- ⚠ Parameter "node_action" in tool "shader" is missing a description.
- ⚠ Parameter "output_node" in tool "shader" is missing a description.
- ⚠ Parameter "output_slot" in tool "shader" is missing a description.
- ⚠ Parameter "input_node" in tool "shader" is missing a description.
- ⚠ Parameter "input_slot" in tool "shader" is missing a description.
- ⚠ Parameter "edge_action" in tool "shader" is missing a description.
- ⚠ Parameter "name" in tool "shader" is missing a description.
- ⚠ Parameter "type" in tool "shader" is missing a description.
- ⚠ Parameter "default_value" in tool "shader" is missing a description.
- ⚠ Parameter "reference_name" in tool "shader" is missing a description.
- ⚠ Parameter "new_name" in tool "shader" is missing a description.
- ⚠ Parameter "layout" in tool "shader" is missing a description.
- ⚠ Parameter "h_gap" in tool "shader" is missing a description.
- ⚠ Parameter "v_gap" in tool "shader" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.target" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.preset" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.code" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.shader_name" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.prop" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.value" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.keyword" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.enabled" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.node_type" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.node_id" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.node_action" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.output_node" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.output_slot" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.input_node" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.input_slot" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.edge_action" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.name" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.type" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.default_value" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.reference_name" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.new_name" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.layout" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.h_gap" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.v_gap" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "target" in tool "shader" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "preset" in tool "shader" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "code" in tool "shader" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "shader_name" in tool "shader" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "prop" in tool "shader" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "value" in tool "shader" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "keyword" in tool "shader" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "enabled" in tool "shader" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "node_type" in tool "shader" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "node_id" in tool "shader" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "node_action" in tool "shader" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "output_node" in tool "shader" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "output_slot" in tool "shader" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "input_node" in tool "shader" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "input_slot" in tool "shader" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "edge_action" in tool "shader" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "name" in tool "shader" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "type" in tool "shader" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "default_value" in tool "shader" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "reference_name" in tool "shader" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "new_name" in tool "shader" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "layout" in tool "shader" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "h_gap" in tool "shader" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "v_gap" in tool "shader" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.target" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.preset" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.code" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.shader_name" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.prop" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.value" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.keyword" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.enabled" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.node_type" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.node_id" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.node_action" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.output_node" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.output_slot" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.input_node" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.input_slot" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.edge_action" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.name" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.type" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.default_value" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.reference_name" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.new_name" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.layout" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.h_gap" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.v_gap" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "shader" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "shader" has 26 parameters. VS Code Copilot performance degrades with more than 15. Consider splitting into multiple tools.
- ⚠ Tool "shader" description is 963 characters. Cline truncates at 200.
- ✖ Tool "shader" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.
- ✖ Schema at "inputSchema.properties.output_slot.anyOf[0]" uses type "integer" which is not allowed by OpenAI Agents SDK. Use one of: string, number, boolean, object, array, null.
- ✖ Schema at "inputSchema.properties.input_slot.anyOf[0]" uses type "integer" which is not allowed by OpenAI Agents SDK. Use one of: string, number, boolean, object, array, null.

#### `smart_build`

- ⚠ Parameter "description" in tool "smart_build" is missing a description.
- ⚠ Tool "smart_build" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "smart_build" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `snapshot`

- ⚠ Parameter "path" in tool "snapshot" is missing a description.
- ⚠ Parameter "label" in tool "snapshot" is missing a description.
- ⚠ Parameter "compare" in tool "snapshot" is missing a description.
- ⚠ Parameter "label" in tool "snapshot" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "compare" in tool "snapshot" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Tool "snapshot" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "snapshot" description is 312 characters. Cline truncates at 200.
- ✖ Tool "snapshot" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `spatial_query`

- ⚠ Parameter "action" in tool "spatial_query" is missing a description.
- ⚠ Parameter "path" in tool "spatial_query" is missing a description.
- ⚠ Parameter "target" in tool "spatial_query" is missing a description.
- ⚠ Parameter "distance" in tool "spatial_query" is missing a description.
- ⚠ Parameter "radius" in tool "spatial_query" is missing a description.
- ⚠ Parameter "component" in tool "spatial_query" is missing a description.
- ⚠ Parameter "cell_size" in tool "spatial_query" is missing a description.
- ⚠ Parameter "layer_mask" in tool "spatial_query" is missing a description.
- ⚠ Parameter "center" in tool "spatial_query" is missing a description.
- ⚠ Parameter "vertices" in tool "spatial_query" is missing a description.
- ⚠ Parameter "region_id" in tool "spatial_query" is missing a description.
- ⚠ Parameter "cap" in tool "spatial_query" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.path" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.target" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.distance" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.radius" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.component" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.cell_size" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.layer_mask" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.center" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.vertices" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.region_id" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.cap" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "path" in tool "spatial_query" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "target" in tool "spatial_query" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "distance" in tool "spatial_query" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "radius" in tool "spatial_query" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "component" in tool "spatial_query" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "cell_size" in tool "spatial_query" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "layer_mask" in tool "spatial_query" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "center" in tool "spatial_query" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "vertices" in tool "spatial_query" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "region_id" in tool "spatial_query" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "cap" in tool "spatial_query" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.path" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.target" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.distance" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.radius" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.component" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.cell_size" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.layer_mask" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.center" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.vertices" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.region_id" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.cap" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "spatial_query" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "spatial_query" description is 762 characters. Cline truncates at 200.
- ✖ Tool "spatial_query" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.
- ✖ Schema at "inputSchema.properties.cap.anyOf[0]" uses type "integer" which is not allowed by OpenAI Agents SDK. Use one of: string, number, boolean, object, array, null.

#### `sync_playtest_aliases_from_defs`

- ⚠ Parameter "defs" in tool "sync_playtest_aliases_from_defs" is missing a description.
- ⚠ Parameter "asset" in tool "sync_playtest_aliases_from_defs" is missing a description.
- ⚠ Parameter "defs" in tool "sync_playtest_aliases_from_defs" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "asset" in tool "sync_playtest_aliases_from_defs" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Tool "sync_playtest_aliases_from_defs" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "sync_playtest_aliases_from_defs" description is 317 characters. Cline truncates at 200.
- ✖ Tool "sync_playtest_aliases_from_defs" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `sync_unity`

- ⚠ Parameter "resolve" in tool "sync_unity" is missing a description.
- ⚠ Parameter "bump" in tool "sync_unity" is missing a description.
- ⚠ Parameter "timeout" in tool "sync_unity" is missing a description.
- ⚠ Parameter "resolve" in tool "sync_unity" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "bump" in tool "sync_unity" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "timeout" in tool "sync_unity" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Tool "sync_unity" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "sync_unity" description is 325 characters. Cline truncates at 200.
- ✖ Tool "sync_unity" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `test_step`

- ⚠ Parameter "path" in tool "test_step" is missing a description.
- ⚠ Parameter "position" in tool "test_step" is missing a description.
- ⚠ Parameter "checks_before" in tool "test_step" is missing a description.
- ⚠ Parameter "checks_after" in tool "test_step" is missing a description.
- ⚠ Parameter "wait_after" in tool "test_step" is missing a description.
- ⚠ Parameter "timeout" in tool "test_step" is missing a description.
- ⚠ Parameter "checks_before" in tool "test_step" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "checks_after" in tool "test_step" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "wait_after" in tool "test_step" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "timeout" in tool "test_step" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Tool "test_step" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "test_step" description is 202 characters. Cline truncates at 200.
- ✖ Tool "test_step" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `timeline`

- ⚠ Parameter "path" in tool "timeline" is missing a description.
- ⚠ Parameter "action" in tool "timeline" is missing a description.
- ⚠ Parameter "track" in tool "timeline" is missing a description.
- ⚠ Parameter "track_type" in tool "timeline" is missing a description.
- ⚠ Parameter "clip" in tool "timeline" is missing a description.
- ⚠ Parameter "binding" in tool "timeline" is missing a description.
- ⚠ Parameter "start" in tool "timeline" is missing a description.
- ⚠ Parameter "duration" in tool "timeline" is missing a description.
- ⚠ Parameter "blend_in" in tool "timeline" is missing a description.
- ⚠ Parameter "blend_out" in tool "timeline" is missing a description.
- ⚠ Parameter "asset_path" in tool "timeline" is missing a description.
- ⚠ Parameter "director_path" in tool "timeline" is missing a description.
- ⚠ Parameter "tracks" in tool "timeline" is missing a description.
- ⚠ Parameter "time" in tool "timeline" is missing a description.
- ⚠ Parameter "name" in tool "timeline" is missing a description.
- ⚠ Parameter "clip_in" in tool "timeline" is missing a description.
- ⚠ Parameter "index" in tool "timeline" is missing a description.
- ⚠ Parameter "offset" in tool "timeline" is missing a description.
- ⚠ Parameter "value" in tool "timeline" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.track" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.track_type" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.clip" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.binding" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.start" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.duration" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.blend_in" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.blend_out" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.asset_path" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.director_path" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.tracks" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.time" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.name" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.clip_in" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.index" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.offset" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.value" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "track" in tool "timeline" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "track_type" in tool "timeline" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "clip" in tool "timeline" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "binding" in tool "timeline" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "start" in tool "timeline" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "duration" in tool "timeline" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "blend_in" in tool "timeline" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "blend_out" in tool "timeline" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "asset_path" in tool "timeline" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "director_path" in tool "timeline" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "tracks" in tool "timeline" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "time" in tool "timeline" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "name" in tool "timeline" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "clip_in" in tool "timeline" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "index" in tool "timeline" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "offset" in tool "timeline" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "value" in tool "timeline" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.track" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.track_type" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.clip" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.binding" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.start" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.duration" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.blend_in" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.blend_out" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.asset_path" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.director_path" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.tracks" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.time" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.name" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.clip_in" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.index" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.offset" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.value" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "timeline" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "timeline" has 19 parameters. VS Code Copilot performance degrades with more than 15. Consider splitting into multiple tools.
- ⚠ Tool "timeline" description is 721 characters. Cline truncates at 200.
- ✖ Tool "timeline" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.
- ✖ Schema at "inputSchema.properties.index.anyOf[0]" uses type "integer" which is not allowed by OpenAI Agents SDK. Use one of: string, number, boolean, object, array, null.

#### `transfer_object`

- ⚠ Parameter "path" in tool "transfer_object" is missing a description.
- ⚠ Parameter "action" in tool "transfer_object" is missing a description.
- ⚠ Parameter "target_scene" in tool "transfer_object" is missing a description.
- ⚠ Parameter "parent" in tool "transfer_object" is missing a description.
- ⚠ Parameter "world_position_stays" in tool "transfer_object" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.target_scene" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.parent" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "target_scene" in tool "transfer_object" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "parent" in tool "transfer_object" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "world_position_stays" in tool "transfer_object" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.target_scene" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.parent" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "transfer_object" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "transfer_object" description is 269 characters. Cline truncates at 200.
- ✖ Tool "transfer_object" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `ui_intent`

- ⚠ Parameter "intent" in tool "ui_intent" is missing a description.
- ⚠ Parameter "parent" in tool "ui_intent" is missing a description.
- ⚠ Parameter "template" in tool "ui_intent" is missing a description.
- ⚠ Parameter "dry_run" in tool "ui_intent" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.parent" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ✖ Keyword "anyOf" at "inputSchema.properties.template" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "parent" in tool "ui_intent" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "template" in tool "ui_intent" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "dry_run" in tool "ui_intent" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.parent" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ✖ Property at "inputSchema.properties.template" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "ui_intent" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "ui_intent" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `undo_last`

- ⚠ Parameter "turns" in tool "undo_last" is missing a description.
- ⚠ Parameter "turns" in tool "undo_last" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Tool "undo_last" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "undo_last" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.
- ✖ Schema at "inputSchema.properties.turns" uses type "integer" which is not allowed by OpenAI Agents SDK. Use one of: string, number, boolean, object, array, null.

#### `unwire_event`

- ⚠ Parameter "path" in tool "unwire_event" is missing a description.
- ⚠ Parameter "component" in tool "unwire_event" is missing a description.
- ⚠ Parameter "event" in tool "unwire_event" is missing a description.
- ⚠ Parameter "index" in tool "unwire_event" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.index" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "index" in tool "unwire_event" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.index" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "unwire_event" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "unwire_event" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.
- ✖ Schema at "inputSchema.properties.index.anyOf[0]" uses type "integer" which is not allowed by OpenAI Agents SDK. Use one of: string, number, boolean, object, array, null.

#### `use_skill`

- ⚠ Parameter "name" in tool "use_skill" is missing a description.
- ⚠ Parameter "params" in tool "use_skill" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.params" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "params" in tool "use_skill" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.params" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "use_skill" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "use_skill" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `validate_layout`

- ⚠ Parameter "root" in tool "validate_layout" is missing a description.
- ⚠ Parameter "min_distance" in tool "validate_layout" is missing a description.
- ⚠ Parameter "root" in tool "validate_layout" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "min_distance" in tool "validate_layout" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Tool "validate_layout" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "validate_layout" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `validate_playtest_aliases`

- ⚠ Parameter "defs" in tool "validate_playtest_aliases" is missing a description.
- ⚠ Parameter "asset" in tool "validate_playtest_aliases" is missing a description.
- ⚠ Parameter "defs" in tool "validate_playtest_aliases" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "asset" in tool "validate_playtest_aliases" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Tool "validate_playtest_aliases" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "validate_playtest_aliases" description is 336 characters. Cline truncates at 200.
- ✖ Tool "validate_playtest_aliases" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `validate_references`

- ⚠ Parameter "path" in tool "validate_references" is missing a description.
- ⚠ Parameter "depth" in tool "validate_references" is missing a description.
- ⚠ Parameter "verbose" in tool "validate_references" is missing a description.
- ⚠ Parameter "ignore_optional" in tool "validate_references" is missing a description.
- ⚠ Parameter "depth" in tool "validate_references" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "verbose" in tool "validate_references" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "ignore_optional" in tool "validate_references" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Tool "validate_references" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "validate_references" description is 347 characters. Cline truncates at 200.
- ✖ Tool "validate_references" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.
- ✖ Schema at "inputSchema.properties.depth" uses type "integer" which is not allowed by OpenAI Agents SDK. Use one of: string, number, boolean, object, array, null.

#### `verify_after_change`

- ⚠ Parameter "changed_files" in tool "verify_after_change" is missing a description.
- ⚠ Parameter "test_filter" in tool "verify_after_change" is missing a description.
- ⚠ Parameter "run_tests_mode" in tool "verify_after_change" is missing a description.
- ⚠ Parameter "playtests" in tool "verify_after_change" is missing a description.
- ⚠ Parameter "mark_id" in tool "verify_after_change" is missing a description.
- ⚠ Parameter "timeout" in tool "verify_after_change" is missing a description.
- ⚠ Parameter "restart_between" in tool "verify_after_change" is missing a description.
- ⚠ Parameter "changed_files" in tool "verify_after_change" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "test_filter" in tool "verify_after_change" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "run_tests_mode" in tool "verify_after_change" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "playtests" in tool "verify_after_change" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "mark_id" in tool "verify_after_change" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "timeout" in tool "verify_after_change" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "restart_between" in tool "verify_after_change" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Tool "verify_after_change" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "verify_after_change" description is 457 characters. Cline truncates at 200.
- ✖ Tool "verify_after_change" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `vfx_intent`

- ⚠ Parameter "target" in tool "vfx_intent" is missing a description.
- ⚠ Parameter "intent" in tool "vfx_intent" is missing a description.
- ⚠ Parameter "kind" in tool "vfx_intent" is missing a description.
- ⚠ Parameter "dry_run" in tool "vfx_intent" is missing a description.
- ⚠ Parameter "kind" in tool "vfx_intent" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "dry_run" in tool "vfx_intent" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Tool "vfx_intent" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "vfx_intent" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `wait_until`

- ⚠ Parameter "path" in tool "wait_until" is missing a description.
- ⚠ Parameter "component" in tool "wait_until" is missing a description.
- ⚠ Parameter "field" in tool "wait_until" is missing a description.
- ⚠ Parameter "value" in tool "wait_until" is missing a description.
- ⚠ Parameter "timeout" in tool "wait_until" is missing a description.
- ⚠ Parameter "negate" in tool "wait_until" is missing a description.
- ⚠ Parameter "abort_on_fail" in tool "wait_until" is missing a description.
- ⚠ Parameter "timeout" in tool "wait_until" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "negate" in tool "wait_until" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "abort_on_fail" in tool "wait_until" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Tool "wait_until" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "wait_until" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

#### `watch`

- ⚠ Parameter "action" in tool "watch" is missing a description.
- ⚠ Parameter "watch_id" in tool "watch" is missing a description.
- ⚠ Parameter "path" in tool "watch" is missing a description.
- ⚠ Parameter "component" in tool "watch" is missing a description.
- ⚠ Parameter "field" in tool "watch" is missing a description.
- ⚠ Parameter "condition" in tool "watch" is missing a description.
- ⚠ Parameter "trigger_action" in tool "watch" is missing a description.
- ⚠ Parameter "interval_ms" in tool "watch" is missing a description.
- ⚠ Parameter "watch_id" in tool "watch" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "path" in tool "watch" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "component" in tool "watch" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "field" in tool "watch" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "condition" in tool "watch" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "trigger_action" in tool "watch" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "interval_ms" in tool "watch" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Tool "watch" is missing a `title`. Cursor can display raw tool names without it.
- ✖ Tool "watch" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.
- ✖ Schema at "inputSchema.properties.interval_ms" uses type "integer" which is not allowed by OpenAI Agents SDK. Use one of: string, number, boolean, object, array, null.

#### `wire_event`

- ⚠ Parameter "path" in tool "wire_event" is missing a description.
- ⚠ Parameter "component" in tool "wire_event" is missing a description.
- ⚠ Parameter "event" in tool "wire_event" is missing a description.
- ⚠ Parameter "target" in tool "wire_event" is missing a description.
- ⚠ Parameter "method" in tool "wire_event" is missing a description.
- ⚠ Parameter "arg_type" in tool "wire_event" is missing a description.
- ⚠ Parameter "arg_value" in tool "wire_event" is missing a description.
- ✖ Keyword "anyOf" at "inputSchema.properties.arg_value" is not in the MCP-supported JSON Schema subset and is not reliably supported across clients.
- ⚠ Parameter "arg_type" in tool "wire_event" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ⚠ Parameter "arg_value" in tool "wire_event" has a `default` value that Continue.dev will ignore. Document the default in the description instead.
- ✖ Property at "inputSchema.properties.arg_value" has a `default` value but no `type`. Cursor requires an explicit type when defaults are present.
- ⚠ Tool "wire_event" is missing a `title`. Cursor can display raw tool names without it.
- ⚠ Tool "wire_event" description is 442 characters. Cline truncates at 200.
- ✖ Tool "wire_event" is missing `additionalProperties: false` on inputSchema. Required for OpenAI Agents SDK strict mode.

</details>
