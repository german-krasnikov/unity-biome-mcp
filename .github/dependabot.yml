version: 2

updates:
  # Python dependencies (uv, not pip: this project pins deps via
  # server/uv.lock, and only the uv ecosystem updates pyproject.toml and
  # uv.lock together in the same PR -- pip-ecosystem bumps left uv.lock
  # stale, see PR #92)
  - package-ecosystem: "uv"
    directory: "/server"
    schedule:
      interval: "weekly"
      day: "monday"
    commit-message:
      prefix: "chore"
    labels:
      - "dependencies"
      - "python"
    reviewers:
      - "german-krasnikov"
    open-pull-requests-limit: 5
    groups:
      mcp-sdk:
        patterns:
          - "mcp"
      testing:
        patterns:
          - "pytest*"
    ignore:
      # mcp pinned <2 — skip major bumps
      - dependency-name: "mcp"
        update-types: ["version-update:semver-major"]

  # GitHub Actions
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
    commit-message:
      prefix: "chore"
    labels:
      - "dependencies"
      - "ci"
    reviewers:
      - "german-krasnikov"
    open-pull-requests-limit: 5
    groups:
      all-actions:
        patterns:
          - "*"
